# 系统审查与修复记录（Q1-Q11）

> 对应审查报告：`docs/19-起名服务代码审查报告.md`
> 修复版本：`2026.09.12.0`

## 修复总览

| 编号 | 问题 | 状态 | 修复说明 |
| ---- | ---- | ---- | -------- |
| Q1 | `cmd/server` 编译失败（`database.Store` 缺 `NameStatStore`） | ✅ | `sqlite.Store` 补齐 4 个方法：`GetSurnameStats`/`GetSurnameStat`/`GetGivenNameStats`/`GetFullNameStats` |
| Q2 | `sqlite.Store` 8 个占位方法静默返回 nil | ✅ | 全部改为返回显式错误 `errNameStatsUnavailable`（中文错误信息，注明当前数据源为 data 目录 JSON） |
| Q3 | `SearchHanziByBasicParams` 空方法名不匹配 `SQLiteCharStore` | ✅ | 重命名为 `SearchHanziByFilter`，`var _ services.SQLiteCharStore = (*Store)(nil)` 守卫编译期强制 |
| Q4 | `queryViaSQL` 过滤逻辑缺陷（genderHint 按拼音比较、wuxingNotIn/namingCategory 漏过滤、多值 wuxingIn 只取首值、regular 排除表外字） | ✅ | 重写为"SQL 粗筛 + Go 以内存 `HanziData` 权威源二次过滤"；新增 SQL/Go 一致性测试 |
| Q5 | 双管线喜用神不一致 + GenerateWithAnalysis 响应缺卦象/紫薇 | ✅ | (1) `FateNameService` 注入经典 `BaziAnalyzer`，无 `WuxingMatch` 时按经典喜用神收窄；(2) 注入卦象/紫微 finder，响应补齐 `Hexagram`/`Ziwei` |
| Q6 | `memory.Store` stub 未显式失败 | ✅ | 同 Q2，统一 `errNameStatsUnavailable` |
| Q7 | 改动文件未 gofmt | ✅ | 全部业务文件 `gofmt -w`（含 import 顺序） |
| Q8 | `namestatistics` 无单元测试 | ✅ | 新增 `service_test.go`（透传+错误传播）、`file_store_test.go`（真查询） |
| Q9 | 重复模块 `namestat`（旧，单字频率）与 `namestatistics`（新，语料统计） | ✅ | 二者数据源与职责不同（前者单字频率、后者人名语料统计），保留并明确分工；`/namestats/*` 改用 JSON 数据源真实现 |
| Q10 | `filter_integration_test.go` "避开 pre-existing 故障"注释 | ✅ | 移除注释，改为真实接口守卫 + Go/SQL 结果等集断言 |
| Q11 | 遗留 `/names/:id` GetByID 死代码 + `rater.go` 注释 7 维 | ✅ | 移除路由/handler/接口方法（前端未使用）；注释修正为"八维" |

## 附加修复

- **classics 竞态**：`loadShiCiAsync` 对全局 `shiciReady` 通道重复 `close`（两个测试先后 `data.Init` 触发 `close of closed channel` panic），用 `sync.Once` 修复。
- **SQLite 汉字字段权威源策略**：`database.Hanzi` 仅含 6 个召回字段，`hanzi_data` 表虽含全列但不承担完整属性返回；SQL 路径候选召回后统一以内存 `hanzi.HanziData` 回查补全（删除原 6 字段映射的 `databaseHanziToHanzi`）。

## 审查遗留小项（docs/19 B7/B11/B12/P4）

| 编号 | 问题 | 状态 | 修复说明 |
| ---- | ---- | ---- | -------- |
| B7 | `BadPinyinCombos` 过宽误判：`da dai`（打的）、`huang se`（黄色）为中性词 | ✅ | 移除两条误判，保留明确负面项 `se qing`（色情） |
| B11 | `totalCount` 注释语义与实现不符 | ✅ | 补注释明确语义为"已评分组合数"（被过滤/早停跳过的组合不计数）；字段供统计展示、前端未消费 |
| B12 | `BadHomophones` 中 `"fu2"`（妇）带数字后缀永不匹配 | ✅ | 改 `"fu"`；并修复 `CheckBadHomophone` 同拼音多词条（fu=腐/妇）首条不中即 `return false` 漏检后续词条缺陷 |
| P4 | `RateName` 封顶重算每次线性扫描 raters 查权重 | ✅ | 预计算 `rateWeightsByDim` 映射，封顶查询降为 O(1) |

> P5（ExcellentTable.New 未复用）未变更：主表/合并表按请求生命周期新建属正确语义，相关容量优化已由 B2/方案5 完成。

## 端到端验证实证（2026.09.12 真实启动 `cmd/server`）

| 端点 | 结果 |
| ---- | ---- |
| `GET /healthz` | `{"status":"ok"}` |
| `POST /api/v1/names/generate` | 200：50 个候选，含完整分维评分（score/items）；`data` 含 `bazi/nayin/zodiac/hexagram/hexagram_match/ziwei` |
| `POST /api/v1/names/generate/analysis` | 200：50 个候选 + `hexagram.name` 非空 + `ziwei`（tianzhu/diwei/lu/quan/ke/ji/zhushen/fuji/xingyao/gongwei/analysis）非空 —— **Q5 卦象/紫薇闭环** |
| `GET /api/v1/namestats/total` | `{"total_names":957746}` —— **Q8/Q9 JSON 数据源闭环** |
| `GET /api/v1/namestats/surnames` | 20 条语料姓氏统计（首条 `王:53987`） |
| `GET /api/v1/namestats/top` | 3 条高频全名（`张伟`/`王伟`/`刘伟`） |

输入：`{surname: 王, gender: male, birth_year: 2024, birth_month: 1, birth_day: 15, birth_hour: 12, name_length: 2, count: 5}`。

## 验证结果

- `go build ./...` 全仓编译通过（此前 `cmd/server`、`database/sqlite` 编译失败）。
- `go test ./...` 全部包 PASS（含 `sqlite` 集成测试、`services` e2e、`handlers`、`namestatistics`）。
- 新增测试：
  - `sqlite/filter_integration_test.go`（genderHint/wuxingNotIn/namingCategory 等集 + BasicParams 5 场景）
  - `namestatistics/service_test.go`、`namestatistics/file_store_test.go`
  - `handlers/handlers_test.go::TestNameStatisticsHandler`
  - `services/fate_name_service_e2e_test.go` Hexagram/Ziwei 断言

## Git 提交

- `bb0b2d4` fix(database): 恢复构建并修正 SQLite 汉字过滤下沉（Q1/Q2/Q3/Q4/Q6/Q10）+ classics 竞态
- `52e0338` fix(services): GenerateWithAnalysis 经典喜用神收窄与 Generate 对齐（Q5）
- `f9b8e4d` feat: 服务装配收尾：SQLite 下沉启用、姓名统计模块、Q5 注入、诗词缓存
- `35c2c55` fix(services): GenerateWithAnalysis 补齐卦象/紫微（Q5）、清理 GetByID（Q11）、namestats JSON 数据源（Q8/Q9）
- `bdfd44c` docs: 更新 CHANGELOG [2026.09.12.0] 并新增 docs/21 审查修复记录
-（下个 commit：B7/B12/P4 修复 + B11 注释 + e2e 实证文档）